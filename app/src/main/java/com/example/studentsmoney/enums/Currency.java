package com.example.studentsmoney.enums;

/**
 * This is a enum for working with currency.
 * Now, we have only 3 types of currency and 4-th
 * ({@link Currency#OTHER}) as default. But, in future,
 * we can add more. (Or change the enum to something else)
 * @author MarkYav
 * @version 1.0
 * @since 2021-01-25
 */
public enum Currency {
    UAH,
    USD,
    EUR,
    OTHER
}
